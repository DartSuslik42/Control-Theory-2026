# Задача 1 — передаточная функция

Найти передаточную функцию системы

```text
W(s) = y(s) / u(s).
```

В решении показать переход от модели в пространстве состояний к `W(s)` и подставить своё значение `n`.

